import { ResaltarEstado } from './resaltar-estado';

describe('ResaltarEstado', () => {
  it('should create an instance', () => {
    const directive = new ResaltarEstado();
    expect(directive).toBeTruthy();
  });
});
